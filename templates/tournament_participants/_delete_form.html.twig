<form method="post" action="{{ path('tournament_participants_delete', {'id': tournament_participant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournament_participant.id) }}">
    <button class="btn">Delete</button>
</form>
