{% extends 'base.html.twig' %}

{% block title %}TournamentParticipants{% endblock %}

{% block body %}
    <h1>TournamentParticipants</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tournament_participant.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tournament_participants_index') }}">back to list</a>

    <a href="{{ path('tournament_participants_edit', {'id': tournament_participant.id}) }}">edit</a>

    {{ include('tournament_participants/_delete_form.html.twig') }}
{% endblock %}
